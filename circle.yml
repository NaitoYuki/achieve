machine:
  timezone:
    Asia/Tokyo
  ruby:
    version:
      2.3.1
dependencies:
  override:
    - bundle install:
    - npm install:
database:
test:
  override:
    - bundle exec rspec spec/
